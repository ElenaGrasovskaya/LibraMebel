import { ScriptsPluginOptions } from './types';
declare type BundleOptions = Required<Omit<ScriptsPluginOptions, 'addWatchTarget'>>;
/** Compile, bundle and minify script. */
export declare const bundle: (html: string, outputPath: string, options: BundleOptions) => Promise<string>;
export {};
