import { Options } from 'sass';
export declare type SassCompilerOptions = Options;
/**
 * Transform Sass to CSS.
 * It adjusts all Sass files into one CSS file.
 */
export declare const compile: (url: string, options?: SassCompilerOptions) => import("sass").Result;
