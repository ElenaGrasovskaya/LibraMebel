export declare const pathStats: (url: string) => {
    directories: string[];
    isAbsolute: boolean;
    isRelative: boolean;
};
export declare const resolveFile: (url: string, inputDirectory: string, relativeDirectory: string) => string;
export declare const buildOutputUrl: (url: string, publicDirectory: string) => string;
