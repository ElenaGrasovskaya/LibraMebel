import { CompressedContentInfo } from './types';
/** Compress content with `brotli` algorithm. */
export declare const brotli: (content: string, outputPath: string) => Promise<CompressedContentInfo>;
