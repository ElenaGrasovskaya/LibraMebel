import { CompressAlgorithm } from './types';
/**
 * Perform compression of HTML file, styles
 * and scripts that are referenced by this HTML.
 */
export declare const compressHTMLWithLinks: (content: string, outputPath: string, algorithm: CompressAlgorithm | ReadonlyArray<CompressAlgorithm>, buildDirectory: string) => Promise<void>;
