import { CompressedContentInfo } from './types';
/** Compress content with `deflate` algorithm. */
export declare const deflate: (content: string, outputPath: string) => Promise<CompressedContentInfo>;
