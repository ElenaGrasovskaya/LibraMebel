/** Build options for raster image optimizer. */
export declare const getRasterOptimizerOptions: (input: string, outputDirectory: string, options?: object) => {
    widths: null[];
    svgShortCircuit: boolean;
    formats: readonly string[];
    outputDir: string;
    urlPath: string;
    sharpPngOptions: {
        quality: number;
        progressive: boolean;
    };
    sharpJpegOptions: {
        quality: number;
        progressive: boolean;
    };
    sharpWebpOptions: {
        quality: number;
        nearLossLess: boolean;
    };
    sharpAvifOptions: {
        quality: number;
    };
    filenameFormat: (id: string, src: string, width: string, format: string, options: object) => string;
};
