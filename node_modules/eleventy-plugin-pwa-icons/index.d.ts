import { PWAIconsOptions } from './transform_html';
/**
 * Generates splash screens and icons, favicons, mstile images for PWA.
 * Also fills `icons` property in the `manifest.json` file.
 */
export declare const icons: (config: Record<string, Function>, { logger, icons, manifest, generatorOptions, enabled, }?: PWAIconsOptions) => void;
export { PWAIconsOptions };
