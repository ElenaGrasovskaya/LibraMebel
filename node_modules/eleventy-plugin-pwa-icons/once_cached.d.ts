/**
 * Executes function only once.
 * If there will be multiple invocations,
 * then cached result will be returned.
 */
export declare const onceCached: <F extends (...args: ReadonlyArray<any>) => any>(fn: F) => F;
