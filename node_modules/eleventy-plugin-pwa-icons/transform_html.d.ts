import { TransformOptions } from './types';
import { Options, LoggerFunction } from './generate_images';
export declare type PWAIconsOptions = TransformOptions & {
    logger?: LoggerFunction;
    /** Decide whether plugin should do its work. */
    enabled?: boolean;
    generatorOptions?: Options;
};
/**
 * Transform function that inserts links of PWA images
 * into HTML file and updates manifest file.
 */
export declare const generateAndInsertIcons: (html: string, buildDirectory: string, { icons, manifest, generatorOptions: options, logger }: PWAIconsOptions) => Promise<string>;
